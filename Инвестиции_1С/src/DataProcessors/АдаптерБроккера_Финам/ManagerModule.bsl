
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

Функция ПолучитьДанные(ТипДанных, Параметры) Экспорт
    // ТипДанных - Перечисление.ТипыДанныхБрокеров
    // Параметры - Структура (аналогично БрокерыДиспетчер)
    // Возвращает ТаблицуЗначений
КонецФункции

Функция ПолучитьНастройкиАдаптера() Экспорт
    // Возвращает ТаблицуЗначений с колонками:
    // - ИмяПараметра (Строка)
    // - ТипПараметра (Строка: "Строка", "Число", "Булево")
    // - Заголовок (Строка)
    // - Обязательный (Булево)
    // Используется для динамического формирования формы настроек
КонецФункции

// Возвращает ТаблицуЗначений с колонками:
// - Дата (Дата)
// - Время (Дата)
// - НомерСделки (Число)
// - НомерЗаявки (Число)
// - ТипСделки (Перечисление.ТипыСделок)
// - Количество (Число)
// - Цена (Число)
// - Сумма (Число)
// - Комиссия (Число)
Функция ПолучитьДанныеПоСделкамИзФайла(ПутьКФайлу) Экспорт
    // ПутьКФайлу - Строка
    // Возвращает ТаблицуЗначений
КонецФункции


#КонецОбласти

#Область ОбработчикиСобытий

// Код процедур и функций

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Код процедур и функций

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Код процедур и функций

#КонецОбласти

#КонецЕсли
